* 순환 의존성은 두 모듈이 임포트 시점에 서로 호출할 때 발생
 > 이런 의존성은 프로그램이 시작될 때 동작을 멈추는 원인이 됨

 * 루트 예외 정의하면 api로부터 사용자를 보호할 수 있음

 * Profile 객체의 runcall 메서드는 함수 호출 트리를 프로파일하는 데 필요한 모든 기능을 제공

 * tracemalloc은 파이썬 3.4 이후 버전부터 사용할 수 있음